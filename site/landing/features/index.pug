head
    include /layout/headFrag

    script(src='/layout/ui-loader.js')
body
    .container
        include /includes/navbar

    .container.py-3
        .row
            .col-md-12.text-center
                h3.mb-3.pb-3 Features
            .col-md-10.offset-md-1.text-center
                include:metaMD /text/easy.md
            .col-md-12.pt-3
                .row
                    .col-md-5
                        .feat-list
                            include:metaMD /text/features1.md

                    .col-md-5.offset-md-1
                        .feat-list
                            include:metaMD /text/features2.md
    include /includes/footer


    style.
        body {
            counter-reset: lic;
        }
        ol {
            list-style-type: none;
        }
        ol > li {
            position:relative; /* Create a positioning context */
            margin:0 0 6px 2em; /* Give each list item a left margin to make room for the numbers */
            padding:4px 8px; /* Add some spacing around the content */
            padding-left: 4px;
            min-height: 50px;
        }
        ol > li:before {
            /* Position and style the number */
            position:absolute;

            counter-increment:lic; /* Increment the counter by 1 */
            content:counter(lic); /* Use the counter as content */

            top:8px;
            left:-44px;
            width:2rem;
          
            text-align:center;
            color:#fff;
            
            font: normal 18px/1.5 "Helvetica Neue", Arial, sans-serif;
            background: #343a40;
            border-radius: 4px;
            font-weight: 300;
        }

