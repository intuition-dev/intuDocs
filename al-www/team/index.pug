extends /layout/layout
//- App Shell

block head2
  
block top
   script(src=ROOT+ 'team/comps/card-comp.js')

   style.
      .pageBack {
         background-image: url(img.jpg);
      }


   .pageBack.delayShowing(data-js-menu-highlight="team")
      include /layout/nav
      br
      H1.flex.white.fLarge.fBod Team

block main
   .pad
      card-tag

   script.
      axios.get('/team/items.json').then(function(resp) {
         team = resp.data;
         console.info('t ready');
         
         const tag = riot.mount('card-tag')[0]; // mount the first tag
         console.info(team.items);
         tag.render(team.items);
      });