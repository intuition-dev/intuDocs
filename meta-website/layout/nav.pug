script(src='/layout/navJBar.js')
script(type='text/javascript', async defer, src='https://assets.pinterest.com/js/pinit.js')

header.delayShowing
   .logo-wrap
      a(href='/')#logo
         span Meta
         | bake
      .site-title Metabake is the extensible low-code productivity tool that simplifies the process for building great software.
   .right
      ul#navJBar.hide-lg.navJBar(data-function='navJBar')
         li
            a.why-metabake(href='#') Why Metabake
            ul
               li
                  a(href='/why-metabake/why-metabake') Why Metabake
               li
                  a(href='/why-metabake/metabake-vs-other-low-code') Metabake vs. Other Low Code
               li
                  a(href='/why-metabake/metabake-vs-hugo') Metabake vs. Hugo
               li
                  a(href='/why-metabake/metabake-vs-gulp-and-grunt') Metabake vs. Gulp and Grunt
         li
            a.who-benefits(href='/who-benefits') Who Benefits
         li
            a.get-started(href='/get-started') Get Started
         li
            a.about(href='#') about
            ul
               li
                  a(href='/about/about/') About Us
               li
                  a(href='/about/vic-cekvenich/') Victor Cekvenich
               li
                  a(href='/about/support/') support
               li
                  a(href='/about/contact/') contact us
      .show-lg#navBarCont
         svg.featherSVG(viewBox='0 0 24 24')
            line(x1='3', y1='12', x2='21', y2='12')
            line(x1='3', y1='6',  x2='21', y2='6')
            line(x1='3', y1='18', x2='21', y2='18')

#navOpen.show-lg

script.
   console.log('navJBar')
   new Navbar('.navJBar') // top
   //toggle
   $( "#navBarCont" ).click(function() { //side
      console.log(isOpen)
      if(isOpen)
         navClose()
      else
         navOpen()
   })

   isOpen = false
   function navOpen() {
      let $clone= $("#navJBar").clone(true)
      $clone.removeClass('hide-lg')
      $('#navOpen').html($clone)
      new Navbar('#navOpen')
      $('.navFirst').addClass('open')
      $('.navFirst').addClass('open-position')
      isOpen = true
   }
   function navClose() {
      $('#navOpen').empty()
      $('.navFirst').removeClass('open')
      $('.navFirst').removeClass('open-position')
      isOpen = false
   }

   setup();